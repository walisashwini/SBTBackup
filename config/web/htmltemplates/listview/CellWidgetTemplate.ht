<div ${innerCellClasses} ${innerCellStyles}>
    <div :wrapIf="isMultipleContentCell" class="gw-multi-content-cell">
        ${:children}
    </div>
    <div :if="allowsDragging" class="gw-drag-bar" data-gw-draggable="gwDraggableColumn" role="separator"></div>
</div>

package gw.account

/**
 * An Adapter interface defining a PendingUpdatesWorkItem to apply updates to account data.
 */
@Export
structure PendingUpdates {

  /**
   * Apply the pending updates.
   */
  function applyUpdates()
}